<div class="caps-view">
    <div>
        @for (int i = 0; i < 3; i++)
        {
            @Html.Partial("_ContestCapItem")
        }
    </div>
    <div>
        @for (int i = 0; i < 3; i++)
        {
            @Html.Partial("_ContestCapItem")
        }
    </div>
</div>